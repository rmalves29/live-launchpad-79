\n## Correção do erro `column o.pix_discount does not exist`\n\n### Causa\nA migração `20260509_fix_orders_rls_secure_rpc.sql` criou a RPC `get_orders_by_phone_public` declarando no `RETURNS TABLE` duas colunas que **não existem** na tabela `orders`:\n- `pix_discount numeric`\n- `merged_order_ids jsonb`\n\nQuando o checkout público (`/t/<slug>/checkout`) chama a RPC ao buscar pedidos por telefone, o Postgres aborta com `column o.pix_discount does not exist` e a tela mostra o toast vermelho.\n\n### Verificação feita\n- Listei todas as colunas reais de `orders` — confirmado: nem `pix_discount` nem `merged_order_ids` existem\n- Busquei no frontend (`PublicCheckout.tsx` linhas 1012 e 1184) — a RPC é chamada 2x, mas o resultado **nunca lê** `.pix_discount` nem `.merged_order_ids`. Só usa: `id, customer_phone, customer_name, event_type, event_date, total_amount, is_paid, is_cancelled, payment_link, cart_id, coupon_code, coupon_discount, gift_name, created_at`\n- O desconto PIX real vem das tabelas `integration_*` (já carregado separado, linhas 296-315) e fica gravado dentro de `observation` como `[PIX_DISCOUNT] R$ X.XX` — não é coluna da tabela orders\n\n### Fix (1 migração SQL, zero mudança no frontend)\n\nRecriar a função `get_orders_by_phone_public` removendo as duas linhas problemáticas do `RETURNS TABLE` e do `SELECT`:\n\n```sql\nCREATE OR REPLACE FUNCTION get_orders_by_phone_public(\n  p_tenant_slug text,\n  p_customer_phone text\n)\nRETURNS TABLE (\n  id              bigint,\n  tenant_id       uuid,\n  customer_phone  text,\n  custom